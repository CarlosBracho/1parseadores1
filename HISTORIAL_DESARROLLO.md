# HISTORIAL_DESARROLLO.md

| Fecha | Hito | Descripción |
|-------|------|-------------|
| 2026-05-12 | Inicialización de Proyecto | Configuración de entorno, documentación base y limpieza de archivos obsoletos. |
| 2026-05-12 | Refactorización Betbird | Actualización de motor de parseo y normalización de nombres de archivos. |
| 2026-05-13 | Motor CapitalOTB | Migración a Puppeteer para bypass de Akamai y telemetría avanzada. |
| 2026-05-14 | UI WatchandWager | Implementación de Modal AJAX para visualización de resultados en `WatchandWager_ia_creacion.php`. |
| 2026-05-15 | Automatización WatchandWager | Creación de `watchandwager_creador.php` con lógica de Alerta 41 y programación automática. |
| 2026-05-15 | Normalización WatchandWager | Implementado `strtoupper(trim())` y volcados de depuración visual para corregir fallos de emparejamiento. |
| 2026-05-15 | Módulo Visual Diagnóstico | Refactorizado el modal de resultados para incluir desglose de dividendos, detección de Dead Heats y formateo de exóticas. |
| 2026-05-15 | Reingeniería Pools Globales | Reestructurada la captura de dividendos desde el nodo raíz de la API, aplicando filtros de rango atómicos y mapeo de propiedades. |
| 2026-05-17 | Evasión WAF y Perfil Persistente | Implementado almacenamiento persistente de cookies (`bot_profile`), exclusión de banderas nativas de automatización (`--enable-automation`) y atenuación de firmas de detección (`AutomationControlled`) con control inteligente de sesión activa en `bot/inh_bot.js`. |
| 2026-05-17 | Redirección de WebSocket y Supresión de Buffer | Redirigidas tramas del WebSocket desde stdout a archivos JSON indexados en milisegundos (`Date.now()`) bajo la ruta pública de recursos `img/` con aislamiento POSIX `0o644` de permisos en `bot/inh_bot.js`. |
| 2026-05-17 | Resolución de Carrera y Despacho de Modales | Implementada espera condicional competitiva en `page.waitForFunction` (VES vs Iniciar Sesión) y bypass defensivo del modal obligatorio de Pago Móvil en `bot/inh_bot.js`. |
| 2026-05-17 | Intercepción GQL y Formateo stdout | Desactivado almacenamiento en disco (JSON/PNG) e implementada sanitización de transporte `42`, parseo GQL y volcado en línea continua delimitada por puntos (`.`) en `stdout` dentro de `bot/inh_bot.js`. |
| 2026-05-17 | Extracción de trackName y Alertas Telegram | Extracción segura de la propiedad `trackName` en el parser de `bot/inh_bot.js`, envío continuo a través de stdout delimitado por tubería (`|`), y refactorización completa de `web_scraping_inh.php` para almacenar estados en arrays bidimensionales y alertar por Telegram en PHP 8.3 de forma nativa (sin `utf8_encode()`). |
| 2026-05-17 | Alerta de Cierre Manual (Telegram) | Integrado bloque cURL de envío de mensajes en `admin_apertura_cierre.php` bajo las rutas de `admin/` y `new/admin/` con formato dual PHP 5.6 / PHP 8.3, exclusión de `utf8_encode()`, deshabilitación de SSL peer verification y timeouts limitados a 3s para prevención de bloqueos síncronos en servidor Linux de producción. |
| 2026-05-17 | Alerta Cierre Manual HNAC (Telegram) | Integrado bloque cURL de envío de mensajes en `admin_apertura_cierre_hnac.php` bajo las rutas de `admin_hnac/` y `new/admin_hnac/` con formato dual PHP 5.6 / PHP 8.3, exclusión de `utf8_encode()`, deshabilitación de SSL peer verification y timeouts limitados a 3s para prevención de bloqueos síncronos en servidor Linux de producción. |
| 2026-05-18 | Supervisor y Reconexión Tolerante a Fallos | Diseñado e integrado lazo supervisor infinito en `web_scraping_inh.php` utilizando flujos no bloqueantes cross-platform, retardo de 50ms (`usleep`) para estabilización de CPU, monitoreo de inactividad de 120s para mitigar congelamientos, liberación segura de descriptores de archivos de perfil mediante `sleep(5)`, e inyección de escuchadores fail-fast (`page.on('error')`, `page.on('disconnected')`, `browser.on('disconnected')`) con aborto ordenado y salida `process.exit(1)` en `bot/inh_bot.js`. |
| 2026-05-18 | Extracción de Inscritos y Detección de Retirados | Diseñada e implementada extracción de `maxProgramNumber` (ejemplar con el dorsal más alto) e identificación de retirados (`statusId === '2'`) a través de un recorrido recursivo y defensivo de `horsesRacesByRacesId.nodes` en `bot/inh_bot.js`, asignando `0` como fallback si no hay ejemplares retirados. Se expandió el stream a 5 parámetros (`r,Numero,Estatus,Inscritos,Retirados`), adaptando resilientemente la validación de `web_scraping_inh.php` a 5 campos sin alterar el control diferencial de Telegram. |
| 2026-05-18 | Extracción de Dividendos y Soporte de Empates | Diseñada e integrada extracción de ganadores (posición 1) y dividendos `win` en `bot/inh_bot.js` mediante recorrido defensivo de `resultsInhsByRacesId.nodes`, con soporte para empates y fallback de `0`. Se migró el separador a tres puntos (`...`), expandiendo el validador y mapeo de `web_scraping_inh.php` a 7 campos con inicialización condicional segura. |
| 2026-05-18 | Optimización de Salida y Hora de Ejecución | Eliminada la cadena estática de depuración `[TRACK DATA]` en `web_scraping_inh.php` y reemplazada por la hora de ejecución estructurada y dinámica mediante `date('g:i s') . 's'` para lograr telemetría visual limpia e inmediata de la sincronía del raspador. |
| 2026-05-18 | Discriminación de Alertas y Aviso de Arranque | Rediseñada la persistencia de `$estadoCarreras` a matriz multidimensional asociativa (`status`, `retirados`, `ganador`, `dividendo`) en `web_scraping_inh.php` garantizando compatibilidad PHP 5.6/8.3. Se implementó una bandera de inicialización única para despachar una sola notificación a Telegram ("nacionales automaticas activadas") al recibir la primera ráfaga de red, y se añadieron cuatro condicionales atómicas e independientes de intercepción diferencial con cURL DRY sin usar `utf8_encode()`. |
| 2026-05-20 | Activación de Producción y Trazabilidad SQL | Activada la mutación física de retirados en la base de datos MariaDB y la inclusión del procesador de tickets nativo, agregando trazabilidad SQL mediante comentarios internos en `web_scraping_inh.php` y asegurando compatibilidad multiplataforma y retroactiva. |
| 2026-05-21 | Optimización de Latencia — Módulo Retiro/Reintegro HNAC | Eliminados cuellos de botella de N+1 queries en `caballos_lista_hnac.php` (pre-carga unificada de inscritos), N×M queries síncronas en `procesar_ticket_retirados_hnac.php` (consolidación en 1 JOIN cross-taquilla), y directivas de debug en `procesar_ticket_reintegraret_hnac.php`. Reducción de latencia estimada 80–90%. |
| 2026-05-21 | Optimización y Bulk Update en Reintegro HNAC | Refactorizado `procesar_ticket_reintegraret_hnac.php`: consolidada la extracción de tickets en una consulta SELECT JOIN unificada, filtrado en memoria y actualización masiva por lotes (chunks) de 200 con WHERE IN y comentarios de trazabilidad. |
| 2026-05-21 | Automatización de Cierre Transaccional | Implementada intercepción de estatus Abierta->Cerrada (1->2) en `web_scraping_inh.php`, preparando la mutación local de `carrera_hnac` y el insert a la `bitacora` (comentadas por seguridad), controladas por el mapa de bits `alerta_cierre_enviada`. |
| 2026-05-21 | Consistencia de Arranque y Control de Estados Preexistentes | Inyectada evaluación condicional `!isset($viejo['status']) && $statusId === 2` en `web_scraping_inh.php` para contrastar con MariaDB y forzar cierre/bitácora locales comentados. |
| 2026-05-21 | Diagnóstico de Comportamiento Cruzado | Auditadas las implicaciones de concurrencia e I/O de retirados y consistencia de arranque en `web_scraping_inh.php`, verificando el control contra el lazo infinito de procesamiento. |
| 2026-05-21 | Activación de Creaciones Automáticas I/O | Remoción de los comentarios de bloque de la Fase 3 en `web_scraping_inh.php` para activar de forma física la apertura de carreras y plantillas de inscritos en MariaDB 10, con sanitización GetSQLValueString. |
| 2026-05-21 | Pre-carga Automatizada de Resultados y Dividendos Simples | Interceptado el arribo de ganadores y dividendos desde el WebSocket en `web_scraping_inh.php`, aplicando la conversión matemática a base 10 con redondeo a 2 decimales para la taquilla local, preparando localmente las consultas SQL comentadas (est_confirmacion_hnac = 2) y protegiendo el flujo contra I/O redundante vía flag de caché volátil. |
| 2026-05-21 | Activación Total de Escrituras Transaccionales I/O | Removidas quirúrgicamente las barras de comentario de todas las sentencias mysqli_query (cierre en caliente, consistencia de arranque y pre-carga de dividendos) en `web_scraping_inh.php`, pasando al modo operativo de producción real. |
| 2026-05-22 | Módulo de Alertas y Trazabilidad | Creada la tabla `alertas_registros` e implementado sistema de registro de IP multi-entorno para llamados y ejecuciones en `watchandwager_creador.php` y `mtp_betbird.php`. Rediseñado el panel administrativo de alertas con Bootstrap 4 e integrada modal jQuery AJAX interactiva. |
| 2026-05-22 | Ajuste Estético Nativo y Retrocompatibilidad | Removida la estructura de Cards de Bootstrap en el panel de alertas. Se restauró el layout líquido y de alta densidad del sistema sobre el fondo gris `#E5E5E5` clásico, manteniendo compatibilidad dual (PHP 5.6 / 8.3), inyectando un fallback de control de visibilidad JS puro para el modal de historial y cargando explícitamente la librería `alertify` para evitar excepciones ReferenceError en consola. |
| 2026-05-22 | Corrección de Botones en Listado de Alertas | Definida la función JavaScript `chequearEnvio()` y la variable global `statusEnvio` en `admin/alertas_lista.php` para habilitar el envío correcto de formularios sin excepciones JS. |
| 2026-05-22 | Corrección de Modal de Historial en Panel de Alertas | Corregido el botón "HISTORIAL" en `admin/alertas_lista.php` mediante la inyección de estilos CSS auto-contenidos (bypasseando Bootstrap 2.0.3) y una lógica de jQuery simplificada que delega y activa la modal de forma nativa sin depender de la librería JS de Bootstrap. |
| 2026-05-22 | Controladores Nativos de Modal, Desbloqueo de Formularios y Trazabilidad | Migrado el visor modal de historial en `admin/alertas_lista.php` a controladores 100% nativos (abrirHistorial, cerrarHistorial y XMLHttpRequest) sin dependencias de jQuery ni Bootstrap JS. Implementado timeout de 5 segundos en `chequearEnvio()` para autodesbloqueo de formularios. Incorporada la visualización del tiempo transcurrido desde el último llamado y la última ejecución usando subconsultas SQL optimizadas en el Recordset 1 y la función helper `tiempoTranscurridoAlerta()`. |
